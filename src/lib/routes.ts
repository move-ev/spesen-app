export const APP_ROUTES = {
  HOME: '/',
  ADMIN: '/admin',
} as const

export const ADMIN_SETTINGS_ROUTES = {
  BUSINESS_UNIT: '/admin/settings/business-units',
  ACCOUNTING_UNIT: '/admin/settings/accounting-units',
} as const

export const AUTH_ROUTES = {
  LOGIN: '/auth',
} as const
